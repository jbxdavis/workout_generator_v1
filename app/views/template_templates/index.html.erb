<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All templates
    </h1>

    <a href="/templates/new" class="btn btn-block btn-outline-secondary">
      Add a new template
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "template_templates/templates_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Sets
        </th>

        <th>
          Reps
        </th>

        <th>
          Name
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @templates.each do |template| %>
      <tr>
        <!-- Display template.id start -->
        <td>
          <%= template.id %>
        </td>
        <!-- Display template.id end -->

        <!-- Display template.sets start -->
        <td>
          <%= template.sets %>
        </td>
        <!-- Display template.sets end -->

        <!-- Display template.reps start -->
        <td>
          <%= template.reps %>
        </td>
        <!-- Display template.reps end -->

        <!-- Display template.name start -->
        <td>
          <%= template.name %>
        </td>
        <!-- Display template.name end -->

        <!-- Display template.created_at start -->
        <td>
          <%= time_ago_in_words(template.created_at) %> ago
        </td>
        <!-- Display template.created_at end -->

        <!-- Display template.updated_at start -->
        <td>
          <%= time_ago_in_words(template.updated_at) %> ago
        </td>
        <!-- Display template.updated_at end -->

        <td>
          <a href="/templates/<%= template.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @templates, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
